{ config, lib, pkgs, ... }:

{
  # This stub file must be replaced with the hardware configuration
  # generated for your desktop.  After installing NixOS, run
  # `nixos-generate-config --show-hardware-config` on the desktop
  # and copy the resulting `hardware-configuration.nix` here.  Do not
  # attempt to share hardware configuration between machines.
}

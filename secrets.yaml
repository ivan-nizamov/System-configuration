# This file holds secrets for your user configuration.  It should
# never be committed in plain text to a public repository.  Use
# `sops` to encrypt it after adding your real values.
#
# To encrypt, generate Age keys per host and run something like:
# sops --age <desktop_pubkey> --age <server_pubkey> \
#   --encrypt --in-place nix/secrets/server.yaml

gh_token: "ghp_replace-with-your-token"
